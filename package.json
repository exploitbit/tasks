{
  "name": "telegram-task-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Telegram Mini App",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "telegraf": "^4.16.3"
  }
}
